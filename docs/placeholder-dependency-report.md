# Placeholder Dependency Report

Every current placeholder that copy or links will eventually need to replace.
**List for review only — nothing in this report has been modified or replaced.**
Generated from a repo audit (`grep` over `lib`, `app`, `netlify`, `components`) plus the verified Netlify/Kit state.

| # | Placeholder | Where it lives | Replace with | Blocked on |
| --- | --- | --- | --- | --- |
| 1 | **Tracker PDF download link** (`REPLACE_WITH_TRACKER_DOWNLOAD_URL`) | Kit welcome Email 1 (draft) and `docs/welcome-sequence-draft.md`. **Not in app code** — the site never renders a download link; delivery is via email. | The final hosted PDF URL | Final tracker PDF asset |
| 2 | **Tracker preview image** | `public/tracker-preview.png` (generated by `scripts/generate-preview-placeholder.mjs`); referenced in `lib/content.ts` (`preview.src`, OG image) | The real one-page tracker artwork — flat render per 06 §2 / 05 §8 | Final tracker artwork |
| 3 | **Privacy page** | `/privacy` → `app/privacy/page.tsx` + `lib/content.ts` `legal.privacy` | Real, professionally reviewed privacy policy for a global audience covering Kit + Netlify | Legal review |
| 4 | **Terms page** | `/terms` → `app/terms/page.tsx` + `lib/content.ts` `legal.terms` | Real, professionally reviewed terms of service; set governing-law jurisdiction + contact details | Legal review |
| 5 | **Public site URL** | `lib/content.ts:12` — `NEXT_PUBLIC_SITE_URL ?? "https://budgetcraftstudio.com"` | Real production domain; used by canonical/OG/Twitter/sitemap/JSON-LD | Domain decision + set `NEXT_PUBLIC_SITE_URL` in Netlify |

## Notes / caveats (for accurate review)

- **Item 1 lives only in email copy, not the codebase.** The audit found no `REPLACE_WITH`/`TODO`/download-link placeholders in `lib` / `app` / `netlify` / `components`. So replacing the tracker link is a Kit + `docs/welcome-sequence-draft.md` edit, not an app-code change.
- **Item 5 is live-relevant now.** `NEXT_PUBLIC_SITE_URL` is **not set in Netlify** (verified), so the deployed site currently emits canonical/OG/sitemap URLs pointing at the placeholder domain `budgetcraftstudio.com`, not `budgetcraft-free.netlify.app`. Non-blocking for the email work, but it affects SEO/share metadata today.
- **Items 3 & 4 — current state is split.** The **deployed/committed** `/privacy` and `/terms` are bare placeholders. The **working tree** (uncommitted) contains unreviewed boilerplate legal copy from earlier — `M lib/content.ts`, `M app/privacy/page.tsx`, `M app/terms/page.tsx`, `?? components/LegalPage.tsx`. Neither is final; both still require legal review. This report does not resolve or commit those changes.
- **Not placeholders, but adjacent dependencies** (from 10 §13, listed for completeness, not modified): the tracker PDF asset itself; fonts/Canva assets; and the cited-but-missing spec files `07_PINTEREST_CONTENT_ENGINE.md`, `08_SEO_BLOG_ENGINE.md`, and `09` (source of the automation-ceiling / cadence / two-survey facts).

**No files in this report were changed.** Replacement is an operator decision per the handoff boundaries.
